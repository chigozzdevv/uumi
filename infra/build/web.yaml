steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - server/web/Dockerfile
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
substitutions:
  _IMAGE: us-east1-docker.pkg.dev/${PROJECT_ID}/uumi/web:manual
